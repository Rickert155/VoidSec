FROM alpine 

RUN apk add --no-cache fish go git
RUN /usr/bin/fish -c "set -U fish_greeting"
WORKDIR /root
RUN go install -v github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest
RUN cp go/bin/subfinder /usr/local/bin/subfinder
RUN rm -rf /root/*
RUN apk del go git
CMD ["fish"]
